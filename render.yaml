services:
  - type: web
    name: verifio
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.14.3

    aptPackages:
      - tesseract-ocr
      - poppler-utils